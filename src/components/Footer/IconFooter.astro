---
import { Icon } from "astro-icon/components"

interface Props {
    icon: string,
    link: string
}

const prop = Astro.props;
---

<a href={prop.link} target="_blank" aria-label={prop.icon}>
  <Icon class="w-8 h-8 py-1 md:py-0" name=`simple-icons:${prop.icon}`/>
</a>