---
import { Icon } from 'astro-icon/components';
---

<details class="flex justify-center items-center min-w-16 rounded-full hover:bg-midnight-light duration-300">
  <summary class="cursor-pointer flex justify-center items-center font-bold select-none h-[48px] duration-300 gap-1">
    <Icon class="w-10 h-10" name="ph:list-bold"/>
  </summary>
  <div class="popover py-2 absolute left-[100%] w-full border-b-2 shadow-xl">
    <slot/>
  </div>
</details>

<style>
  .popover {
    transform: translate(-100%);
    --col-midnight-transparent: #1f222b88;
    --col-midnight-light-transparent: #33324b88;
    background-image: linear-gradient(70deg, var(--col-midnight-transparent), 75%, var(--col-midnight-light-transparent) 100%);
    backdrop-filter: blur(1rem);
    border-color: #5bbde4;
  }
</style>