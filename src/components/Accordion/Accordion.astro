---
import { Icon } from 'astro-icon/components';

interface Props {
  title: string,
  close?: boolean
}

const prop = Astro.props;
---

<details class="accordion" open={!prop.close}>
  <summary class="accordion-summary flex justify-between items-center">
    {prop.title}
    <Icon class="select" name="ph:triangle-fill"/>
  </summary>
  <slot/>
</details>

<style is:inline>
  details.accordion {
    padding: 1em;
    background-color: var(--col-midnight-light);
    border-radius: 4px;
  }

  .accordion-summary::marker {
    content: "";
  }

  details.accordion[open] .accordion-summary {
    margin-bottom: 10px;
  }

  .accordion-summary {
    outline: none;
    user-select: none;
    transition: margin 150ms ease-out;
    cursor: pointer;
    font-weight: bold;
  }

  .select {
    transition: 300ms;
    transform: rotate(180deg);
  }

  details.accordion[open] .select {
    transform: rotate(360deg);
  }
</style>