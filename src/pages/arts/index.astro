---
import ArtLayout from '../../layouts/ArtLayout.astro';

const pictures: Record<string, any>[] = await Astro.glob("/src/assets/webp_arts/**/*.webp").then(x => x.map(x => x.default));
---

<ArtLayout year="All" {pictures}/>