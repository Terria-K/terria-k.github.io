---
import { stringToDateString } from "../../utils/date";
import Layout from "../../layouts/Layout.astro";
import BlogPost from "../../components/Blog/BlogPost.astro";
import { getCollection } from "astro:content";

const posts = await getCollection("posts");
---

<Layout>
  <main class="flex-auto">
    <ul class="text-white bg-gray-800 px-4 py-8 max-w-[80rem] mx-auto space-y-12">
      {
        posts.reverse().sort((x, y) => {
          // @ts-ignore
          return (new Date(y.data.date) - new Date(x.data.date));
        }).map((x, i) => {
          const dateString = stringToDateString(x.data.date);

          return <li class="list-none">
            <BlogPost tag={x.data.tags} shouldLazy={i > 1} image={x.data.image} 
              date={dateString} title={x.data.title} description={x.data.description} url={x.slug}/>
          </li>
        }

        )
      }
    </ul>
  </main>
</Layout>
