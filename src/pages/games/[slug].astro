---
import { getCollection } from 'astro:content';
import Layout from '../../layouts/Layout.astro';

export async function getStaticPaths() {
  const gamesCollection = await getCollection("games") as any[];

  return gamesCollection.map(x => {
    return {
      params: { slug: x.slug }
    }
  })
}
---

<Layout>

</Layout>